# ── Copy this file to ".env" on the server and fill in real values. ──
# ".env" is gitignored and must NEVER be committed.

# ---- Server ----
PORT=5094

# ---- SQL Server ----
DB_SERVER=2023-DT1
DB_PORT=1433
DB_DATABASE=SmartDeskApp
DB_USER=smartapp_user
DB_PASSWORD=__set_this_on_the_server__

# ---- Server date source ----
# Production MUST derive all dates (TODAY, order-issue MMYY) from this server's
# clock — never from a client. See docs: I10 / F9.
